
<div class='bg-body-secondary p-3'>

  <%= form_with model: @user, url: user_registration_path, class: 'bg-body-secondary ', local: true do |f| %>
  <div class='bg-body container py-3'>
    <div class='form-wrap'>
      <div class='form-header'>
        <h1 class='text-center'>
          会員情報入力
        </h1>
      </div>


      <%= render 'shared/error_messages', model: f.object %>

      <div class="form-group">
        <div class='form-text-wrap'>
          <label class="form-label">ニックネーム</label>
          <span class="text-danger">必須</span>
        </div>
        <%= f.text_area :nickname, class:"form-control", id:"nickname", placeholder:"例) 薬屋太郎", maxlength:"40" %>
      </div>
      <div class="form-group">
        <div class='form-text-wrap'>
          <label class="form-label">メールアドレス</label>
          <span class="text-danger">必須</span>
        </div>
        <%= f.email_field :email, class:"form-control", id:"email", placeholder:"PC・携帯どちらでも可", autofocus: true %>
      </div>
      <div class="form-group">
        <div class='form-text-wrap'>
          <label class="form-label">パスワード</label>
          <span class="text-danger">必須</span>
        </div>
        <%= f.password_field :password, class:"form-control", id:"password", placeholder:"6文字以上の半角英数字" %>
        <p class='form-text'>※英字と数字の両方を含めて設定してください</p>
      </div>
      <div class="form-group">
        <div class='form-text-wrap'>
          <label class="form-label">パスワード(確認)</label>
          <span class="text-danger">必須</span>
        </div>
        <%= f.password_field :password_confirmation, class:"form-control", id:"password-confirmation", placeholder:"同じパスワードを入力して下さい" %>
      </div>
      <div class="form-group">
        <h4 class='form-bottom-text'>
          「新規登録」のボタンを押すことにより、
          <span>利用規約</span>
          <br>に同意したものとみなします
        </h4>
      </div>
      <div class='register-btn'>
        <%= f.submit "新規登録" ,class:"register-red-btn" %>
      </div>
      <%# <p class='register-info'>本人情報の登録について</p> %>
    </div>
    <% end %>
  </div>
</div>