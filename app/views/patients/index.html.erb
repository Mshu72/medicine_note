
<div class='bg-body-secondary p-3'>

  <div class='patient_head'>
    <h4>ようこそ、<%= current_user.nickname %>さん</h4>
  </div>

  <div class='bg-body container py-3'>
    <div class='text-center'>
      <h3 class='text-center'>利用者を選択してください</h3>
      <%= link_to '新規利用者登録',  new_patient_path, class: 'link-dark link-offset-2 link-underline-opacity-0 link-underline-opacity-100-hover' %>
      <% @patients.each do |patient| %>
        <div class="patient">
          <%= link_to patient.patient_name, patient_path(patient.id), class: 'link-dark link-offset-2 link-underline-opacity-0 link-underline-opacity-100-hover'%>
        </div>
      <% end %>
    </div>
    <div class='text-center'>
      <div class='patient'>
      <%= link_to '診察予約',   class: "link-dark link-offset-2 link-underline-opacity-25 link-underline-opacity-100-hover" %>  ※現在調整中です
      </div>
    </div>
  </div>
</div>